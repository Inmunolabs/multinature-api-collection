meta {
  name: UsersList
  type: http
  seq: 4
}

get {
  url: {{userHost}}/users
  body: none
  auth: bearer
}

headers {
  Accept-Language: sp
  ~Accept-Language: en
}

auth:bearer {
  token: {{adminToken}}
}
